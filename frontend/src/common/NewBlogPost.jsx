import {Button} from "react-md";
import React, {PureComponent} from "react";

export default class NewBlogPost extends PureComponent {
    render() {
        return (
            <Button floating secondary>add</Button>
        );
    }
}